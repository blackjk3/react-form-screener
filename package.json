{
  "name": "react-form-screener",
  "version": "0.0.1",
  "description": "A drag and drop form builder for react.",
  "main": "lib/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:js": "babel src/*/*.* -d lib/ && babel src/*.* -d lib/"
  },
  "keywords": [
    "react",
    "react-component",
    "form",
    "builder",
    "drag",
    "drop"
  ],
  "peerDependencies": {
    "react": "^0.14.0 || ^15.0.0"
  },
  "author": "Jason Kadrmas, Aaron Johnson",
  "license": "MIT",
  "dependencies": {
    "classnames": "^2.1.3",
    "react-sortable-items": "0.0.7",
    "reflux": "^0.2.8"
  }
}
